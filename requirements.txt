# Core API
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Validation & config
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# AI / LangChain stack
langchain==0.3.7
# Pin langchain-core: it is not constrained by the meta-packages' loose bounds,
# and an unpinned install drifts to 1.x, which breaks langchain-openai 0.2.9 and
# ragas 0.2.6 (ChatOpenAI stops forwarding api_key; RAGAS returns NaN).
langchain-core>=0.3.15,<0.4
langgraph==0.2.45
langchain-community==0.3.7
langchain-openai==0.2.9
openai==1.54.0
langchain-chroma==0.1.4
# Pin the ollama integration too: 1.x requires langchain-core 1.x and would
# re-introduce the drift above.
langchain-ollama==0.2.3
ollama

# Vector DB & embeddings
chromadb==0.5.20
sentence-transformers==3.3.1

# Data sources — yfinance removed, using EDGAR directly
requests==2.32.3
beautifulsoup4==4.12.3

# Evaluation
ragas==0.2.6
datasets==3.1.0

# Observability
langfuse==2.57.0

# UI
streamlit==1.40.2

# PDF generation
reportlab==4.2.5

# Dev tools — kept here for CI, acceptable in single requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.2
ruff==0.8.3
black==24.10.0
mypy==1.13.0
types-requests