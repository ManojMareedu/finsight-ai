.venv/
.env
__pycache__/
data/
.vscode/
.idea/

# Generated report artifacts (the app returns PDFs via API/UI, not to the repo)
*.pdf

# Timestamped evaluation snapshots are reproducible via `make benchmark` /
# `make product-eval`; only the canonical `_latest` pair is committed as evidence.
evaluation/results/benchmark_2*.json
evaluation/results/benchmark_2*.md
evaluation/results/product_eval_2*.json
evaluation/results/product_eval_2*.md

# Tool caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# macOS Finder metadata
.DS_Store
src/.DS_Store

# Internal remediation audit — kept local
audit/
